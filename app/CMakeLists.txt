set(APP_MAIN_PATH ${CMAKE_CURRENT_SOURCE_DIR}/main)

list(APPEND APP_INCLUDE_DIRS
    ${APP_MAIN_PATH}/
    
)

list(APPEND APP_SRC
    ${APP_MAIN_PATH}/app_main.c
    
)

set(APP_INCLUDE_DIRS ${APP_INCLUDE_DIRS} PARENT_SCOPE)
set(APP_SRC ${APP_SRC} PARENT_SCOPE)